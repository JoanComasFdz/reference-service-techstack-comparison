# ChartGenerator Architecture and Sequence Diagrams

> **For Claude:** This document provides architectural overview and sequence diagrams for the ChartGenerator implementation.

**Related Files:**
- Main Plan: `12.PHASE_3_REPORTING_PLAN.md` (Task 12-14)
- Test Guide: `12.PHASE_3_REPORTING_PLAN.CHARTGENERATOR_TESTS.md`
- Implementation: `PerformanceTester.Reporting/ChartGenerator.cs`
- Tests: `PerformanceTester.Reporting.IntegrationTests/ChartGeneratorTests.cs`

**Status:** 📋 Ready for implementation (Created: 2025-01-14)

---

## Table of Contents

- [Overview](#overview)
- [Architecture Diagram](#architecture-diagram)
  - [Component Structure](#component-structure)
  - [Data Flow](#data-flow)
  - [Dependencies](#dependencies)
- [Sequence Diagram](#sequence-diagram)
  - [Main Generation Flow](#main-generation-flow)
  - [Subplot Creation Flow](#subplot-creation-flow)
  - [Error Handling Flow](#error-handling-flow)
- [Class Diagram](#class-diagram)
- [File System Interaction](#file-system-interaction)
- [ScottPlot Integration Details](#scottplot-integration-details)

---

## Overview

The **ChartGenerator** is responsible for creating comprehensive performance visualization charts that combine throughput, CPU, and RAM metrics across multiple dimensions (service, containers, system-wide). It generates PNG files with 5 subplots that exactly match the Python matplotlib implementation's layout, colors, and formatting.

**Key Responsibilities:**
1. Load metrics data from 6 JSON files
2. Create 5-subplot layout using ScottPlot MultiPlot
3. Plot time-series data with dual Y-axes for resource metrics
4. Apply exact color scheme (16 colors) matching Python implementation
5. Add phase boundary markers and labels
6. Format legends with multiline statistics
7. Export PNG at 2100×2400 pixels (150 DPI equivalent)

---

## Architecture Diagram

### Component Structure

```mermaid
graph TB
    subgraph "Reporting Slice"
        IChartGen[IChartGenerator Interface]
        ChartGen[ChartGenerator Class]
        ChartColors[ChartColors Helper]

        subgraph "Models"
            TestReport[TestReport]
            ThroughputReport[ThroughputReport]
            ResourceReport[ResourceMetricsReport]
            SystemInfo[SystemInfo]
        end
    end

    subgraph "External Libraries"
        ScottPlot[ScottPlot 5.x]
        SkiaSharp[SkiaSharp 2.88.8]
    end

    subgraph "File System"
        JSONFiles[JSON Data Files]
        PNGOutput[PNG Chart File]
    end

    subgraph "DI Container"
        Logger[ILogger&lt;ChartGenerator&gt;]
    end

    IChartGen -.implements.- ChartGen
    ChartGen --> ChartColors
    ChartGen --> TestReport
    ChartGen --> ThroughputReport
    ChartGen --> ResourceReport
    ChartGen --> Logger
    ChartGen --> ScottPlot
    ScottPlot --> SkiaSharp

    ChartGen -.reads.- JSONFiles
    ChartGen -.writes.- PNGOutput

    style ChartGen fill:#3498db,stroke:#2980b9,color:#fff
    style ScottPlot fill:#e74c3c,stroke:#c0392b,color:#fff
    style JSONFiles fill:#95a5a6,stroke:#7f8c8d,color:#fff
    style PNGOutput fill:#2ecc71,stroke:#27ae60,color:#fff
```

### Data Flow

```mermaid
flowchart LR
    subgraph "Input Data"
        Events[events-throughput.json]
        API[api-throughput.json]
        Service[resource-metrics.json]
        RabbitMQ[rabbitmq-metrics.json]
        Postgres[postgres-metrics.json]
        System[system-metrics.json]
        Report[TestReport Config]
    end

    subgraph "ChartGenerator Processing"
        Load[Load & Deserialize JSON]
        Validate[Validate Data]
        CreatePlot[Create MultiPlot 5x1]

        subgraph "Plot Each Subplot"
            SP1[Throughput Events+API]
            SP2[Service CPU/RAM]
            SP3[RabbitMQ CPU/RAM]
            SP4[PostgreSQL CPU/RAM]
            SP5[System CPU/RAM]
        end

        Phases[Add Phase Boundaries]
        Labels[Add Phase Labels]
        Format[Format X-Axis & Legends]
    end

    subgraph "Output"
        PNG[chart.png 2100x2400]
    end

    Events --> Load
    API --> Load
    Service --> Load
    RabbitMQ --> Load
    Postgres --> Load
    System --> Load
    Report --> Validate

    Load --> Validate
    Validate --> CreatePlot
    CreatePlot --> SP1
    CreatePlot --> SP2
    CreatePlot --> SP3
    CreatePlot --> SP4
    CreatePlot --> SP5

    SP1 --> Phases
    SP2 --> Phases
    SP3 --> Phases
    SP4 --> Phases
    SP5 --> Phases

    Phases --> Labels
    Labels --> Format
    Format --> PNG

    style Load fill:#3498db,stroke:#2980b9,color:#fff
    style CreatePlot fill:#9b59b6,stroke:#8e44ad,color:#fff
    style PNG fill:#2ecc71,stroke:#27ae60,color:#fff
```

### Dependencies

```mermaid
graph TB
    subgraph "NuGet Packages"
        ScottPlot[ScottPlot 5.0.52]
        SkiaSharp[SkiaSharp 2.88.8]
        MathNet[MathNet.Numerics 5.0.0]
        JsonSys[System.Text.Json]
    end

    subgraph "Project References"
        Common[PerformanceTester.Common]
        EventPub[PerformanceTester.EventPublishing]
        EventCon[PerformanceTester.EventConsuming]
        ProcMon[PerformanceTester.ProcessMonitoring]
        DockerMon[PerformanceTester.DockerMonitoring]
        ApiLoad[PerformanceTester.ApiLoadTesting]
    end

    subgraph "ChartGenerator"
        ChartGen[ChartGenerator.cs]
        IChartGen[IChartGenerator.cs]
    end

    ChartGen --> ScottPlot
    ChartGen --> MathNet
    ChartGen --> JsonSys
    ChartGen --> Common
    ChartGen --> EventPub
    ChartGen --> EventCon
    ChartGen --> ProcMon
    ChartGen --> DockerMon
    ChartGen --> ApiLoad

    ScottPlot --> SkiaSharp

    style ChartGen fill:#3498db,stroke:#2980b9,color:#fff
    style ScottPlot fill:#e74c3c,stroke:#c0392b,color:#fff
```

---

## Sequence Diagram

### Main Generation Flow

```mermaid
sequenceDiagram
    participant Client
    participant ChartGenerator
    participant FileSystem
    participant JsonDeserializer
    participant ScottPlot
    participant MultiPlot
    participant SkiaSharp

    Client->>ChartGenerator: GenerateChartAsync(outputPath, testReport)

    Note over ChartGenerator: Validation Phase
    ChartGenerator->>ChartGenerator: Validate outputPath
    ChartGenerator->>ChartGenerator: Validate testReport
    ChartGenerator->>FileSystem: Ensure output directory exists

    Note over ChartGenerator: Data Loading Phase
    ChartGenerator->>ChartGenerator: Derive file paths from outputPath
    ChartGenerator->>FileSystem: Check if data files exist
    FileSystem-->>ChartGenerator: File existence status

    alt No data files found
        ChartGenerator-->>Client: InvalidOperationException
    end

    ChartGenerator->>FileSystem: Read events-throughput.json
    FileSystem-->>ChartGenerator: JSON content
    ChartGenerator->>JsonDeserializer: Deserialize<ThroughputReport>
    JsonDeserializer-->>ChartGenerator: ThroughputReport object

    ChartGenerator->>FileSystem: Read api-throughput.json
    FileSystem-->>ChartGenerator: JSON content
    ChartGenerator->>JsonDeserializer: Deserialize<ThroughputReport>
    JsonDeserializer-->>ChartGenerator: ThroughputReport object

    ChartGenerator->>FileSystem: Read resource-metrics.json
    FileSystem-->>ChartGenerator: JSON content
    ChartGenerator->>JsonDeserializer: Deserialize<ResourceMetricsReport>
    JsonDeserializer-->>ChartGenerator: ResourceMetricsReport object

    ChartGenerator->>FileSystem: Read rabbitmq-metrics.json
    ChartGenerator->>FileSystem: Read postgres-metrics.json
    ChartGenerator->>FileSystem: Read system-metrics.json

    Note over ChartGenerator: Chart Creation Phase
    ChartGenerator->>ScottPlot: new MultiPlot(rows: 5, cols: 1)
    ScottPlot-->>ChartGenerator: MultiPlot instance

    ChartGenerator->>MultiPlot: Set Width = 2100
    ChartGenerator->>MultiPlot: Set Height = 2400
    ChartGenerator->>MultiPlot: ShareX = true
    ChartGenerator->>MultiPlot: Set Title

    ChartGenerator->>MultiPlot: GetSubplot(0, 0) [Throughput]
    MultiPlot-->>ChartGenerator: Plot instance
    ChartGenerator->>ChartGenerator: PlotThroughputData(plot, eventsData, apiData)

    ChartGenerator->>MultiPlot: GetSubplot(1, 0) [Service]
    MultiPlot-->>ChartGenerator: Plot instance
    ChartGenerator->>ChartGenerator: PlotResourceData(plot, serviceData, colors)

    ChartGenerator->>MultiPlot: GetSubplot(2, 0) [RabbitMQ]
    ChartGenerator->>MultiPlot: GetSubplot(3, 0) [PostgreSQL]
    ChartGenerator->>MultiPlot: GetSubplot(4, 0) [System]

    Note over ChartGenerator: Phase Boundaries Phase
    ChartGenerator->>ChartGenerator: AddPhaseBoundaries(plots, testReport)
    loop For each subplot
        ChartGenerator->>MultiPlot: Add vertical lines (phase boundaries)
    end

    ChartGenerator->>ChartGenerator: AddPhaseLabels(topPlot, testReport)
    ChartGenerator->>MultiPlot: Add text labels (Consume, API)

    Note over ChartGenerator: Export Phase
    ChartGenerator->>MultiPlot: SavePng(outputPath)
    MultiPlot->>SkiaSharp: Render to PNG
    SkiaSharp->>FileSystem: Write PNG bytes
    FileSystem-->>SkiaSharp: Success
    SkiaSharp-->>MultiPlot: Success
    MultiPlot-->>ChartGenerator: Success

    ChartGenerator->>ChartGenerator: Log success message
    ChartGenerator-->>Client: Task completed
```

### Subplot Creation Flow

```mermaid
sequenceDiagram
    participant ChartGenerator
    participant Plot
    participant ChartColors
    participant ScottPlot

    Note over ChartGenerator: Example: Service CPU/RAM Subplot

    ChartGenerator->>Plot: Set YLabel("Service CPU (%)")
    ChartGenerator->>Plot: Configure Left Axis (bold, size 11)
    ChartGenerator->>ChartColors: Get ServiceCpu color (#3498db)
    ChartColors-->>ChartGenerator: Color object

    Note over ChartGenerator: Plot CPU Data (Left Y-Axis)
    ChartGenerator->>ChartGenerator: Extract timestamps and CPU values
    ChartGenerator->>Plot: Add.Scatter(timestamps, cpuValues)
    Plot-->>ChartGenerator: Scatter plot object
    ChartGenerator->>Plot: Set Color = ServiceCpu
    ChartGenerator->>Plot: Set LineWidth = 2
    ChartGenerator->>ChartGenerator: FormatResourceLegend(cpuSummary)
    ChartGenerator->>Plot: Set LegendText (multiline)

    ChartGenerator->>Plot: Add.FillY(timestamps, cpuValues)
    Plot-->>ChartGenerator: Fill object
    ChartGenerator->>Plot: Set FillColor = ServiceCpu.WithAlpha(0.3)

    ChartGenerator->>Plot: Add.HorizontalLine(cpuSummary.Avg)
    Plot-->>ChartGenerator: Line object
    ChartGenerator->>ChartColors: Get ServiceCpuAvg color (#2980b9)
    ChartColors-->>ChartGenerator: Color object
    ChartGenerator->>Plot: Set Color = ServiceCpuAvg
    ChartGenerator->>Plot: Set LineStyle = Dashed

    Note over ChartGenerator: Plot RAM Data (Right Y-Axis)
    ChartGenerator->>ChartGenerator: Extract timestamps and RAM values
    ChartGenerator->>Plot: Add.Scatter(timestamps, ramValues)
    Plot-->>ChartGenerator: Scatter plot object
    ChartGenerator->>ChartColors: Get ServiceRam color (#e74c3c)
    ChartColors-->>ChartGenerator: Color object
    ChartGenerator->>Plot: Set Color = ServiceRam
    ChartGenerator->>Plot: Set Axes.YAxis = Right

    ChartGenerator->>Plot: Add.FillY(timestamps, ramValues)
    Plot-->>ChartGenerator: Fill object
    ChartGenerator->>Plot: Set FillColor = ServiceRam.WithAlpha(0.2)
    ChartGenerator->>Plot: Set Axes.YAxis = Right

    ChartGenerator->>Plot: Add.HorizontalLine(ramSummary.Avg)
    Plot-->>ChartGenerator: Line object
    ChartGenerator->>ChartColors: Get ServiceRamAvg color (#c0392b)
    ChartColors-->>ChartGenerator: Color object
    ChartGenerator->>Plot: Set Axes.YAxis = Right

    Note over ChartGenerator: Configure Right Axis
    ChartGenerator->>Plot: Axes.Right.Label.Text = "Service RAM (MB)"
    ChartGenerator->>Plot: Axes.Right.Label.ForeColor = ServiceRam
    ChartGenerator->>Plot: Axes.Right.Label.Bold = true

    Note over ChartGenerator: Configure Legend
    ChartGenerator->>Plot: Legend.IsVisible = true
    ChartGenerator->>Plot: Legend.Location = UpperLeft
    ChartGenerator->>Plot: Legend.ManualOffsetX = Width + 150
    ChartGenerator->>Plot: Legend.FontSize = 9
```

### Error Handling Flow

```mermaid
sequenceDiagram
    participant Client
    participant ChartGenerator
    participant FileSystem
    participant Logger

    Client->>ChartGenerator: GenerateChartAsync(null, testReport)
    ChartGenerator->>ChartGenerator: Validate outputPath
    ChartGenerator-->>Client: ArgumentException (null path)

    Client->>ChartGenerator: GenerateChartAsync(outputPath, null)
    ChartGenerator->>ChartGenerator: Validate testReport
    ChartGenerator-->>Client: ArgumentNullException (null report)

    Client->>ChartGenerator: GenerateChartAsync(outputPath, testReport)
    ChartGenerator->>FileSystem: Check data files exist
    FileSystem-->>ChartGenerator: All files missing
    ChartGenerator->>Logger: LogWarning("No data files found")
    ChartGenerator-->>Client: InvalidOperationException

    Client->>ChartGenerator: GenerateChartAsync(outputPath, testReport)
    ChartGenerator->>FileSystem: Read events-throughput.json
    FileSystem-->>ChartGenerator: IOException
    ChartGenerator->>Logger: LogWarning("Failed to load events data")
    ChartGenerator->>ChartGenerator: Continue with other files
    Note over ChartGenerator: Graceful degradation - partial chart

    Client->>ChartGenerator: GenerateChartAsync(outputPath, testReport)
    ChartGenerator->>FileSystem: Read malformed JSON
    FileSystem-->>ChartGenerator: Corrupted content
    ChartGenerator->>ChartGenerator: JsonSerializer.Deserialize
    ChartGenerator->>Logger: LogWarning("Failed to deserialize")
    ChartGenerator->>ChartGenerator: Set data = null, continue

    Note over ChartGenerator: Generate chart with available data only
```

---

## Class Diagram

```mermaid
classDiagram
    class IChartGenerator {
        <<interface>>
        +GenerateChartAsync(outputPath: string, testReport: TestReport, cancellationToken: CancellationToken) Task
    }

    class ChartGenerator {
        -_logger: ILogger~ChartGenerator~
        -_jsonOptions: JsonSerializerOptions
        +ChartGenerator(logger: ILogger~ChartGenerator~)
        +GenerateChartAsync(outputPath: string, testReport: TestReport, cancellationToken: CancellationToken) Task
        -LoadThroughputReport(filePath: string) ThroughputReport?
        -LoadResourceReport(filePath: string) ResourceMetricsReport?
        -PlotThroughputData(plot: Plot, eventsData: ThroughputReport?, apiData: ThroughputReport?) void
        -PlotResourceData(plot: Plot, data: ResourceMetricsReport, cpuLabel: string, ramLabel: string, colors: ColorSet) void
        -AddPhaseBoundaries(plots: Plot[], testReport: TestReport, phaseTimestamps: PhaseTimestamps) void
        -AddPhaseLabels(topPlot: Plot, testReport: TestReport, phaseTimestamps: PhaseTimestamps) void
        -FormatThroughputLegend(prefix: string, summary: ThroughputSummary) string
        -FormatResourceLegend(summary: ResourceSummary) string
    }

    class ChartColors {
        <<static>>
        +EventsPrimary: Color
        +EventsAverage: Color
        +ApiPrimary: Color
        +ApiAverage: Color
        +ServiceCpu: Color
        +ServiceCpuAvg: Color
        +ServiceRam: Color
        +ServiceRamAvg: Color
        +RabbitMqCpu: Color
        +RabbitMqCpuAvg: Color
        +RabbitMqRam: Color
        +RabbitMqRamAvg: Color
        +PostgresCpu: Color
        +PostgresCpuAvg: Color
        +PostgresRam: Color
        +PostgresRamAvg: Color
        +SystemCpu: Color
        +SystemCpuAvg: Color
        +SystemRam: Color
        +SystemRamAvg: Color
        +ConsumePhase: Color
        +ConsumeBoundary: Color
        +ApiPhase: Color
    }

    class TestReport {
        +TestDate: DateTime
        +TotalRuntimeSeconds: double
        +PhaseTimestamps: PhaseTimestamps
        +MonitoredProcess: MonitoredProcess
        +System: SystemInfo
        +Configuration: TestConfiguration
        +Results: TestResults
    }

    class ThroughputReport {
        +TestDate: string
        +SamplingIntervalMs: int
        +Samples: IReadOnlyList~ThroughputSampleJson~
        +Summary: ThroughputSummary
    }

    class ResourceMetricsReport {
        +TestDate: string
        +SamplingIntervalMs: int
        +Samples: IReadOnlyList~ResourceSampleJson~
        +CpuSummary: ResourceSummary
        +MemorySummary: ResourceSummary
    }

    class ThroughputSummary {
        +AvgRate: double
        +PeakRate: double
        +MinRate: double
        +StdDevRate: double
        +CvRate: double
        +AvgResponseTimeMs: double
        +TotalSamples: int
        +TotalCount: int
    }

    class ResourceSummary {
        +Avg: double
        +Min: double
        +Max: double
        +Mode: int
        +Unit: string
    }

    IChartGenerator <|.. ChartGenerator : implements
    ChartGenerator --> TestReport : uses
    ChartGenerator --> ThroughputReport : uses
    ChartGenerator --> ResourceMetricsReport : uses
    ChartGenerator --> ChartColors : uses
    ThroughputReport --> ThroughputSummary : contains
    ResourceMetricsReport --> ResourceSummary : contains
```

---

## File System Interaction

```mermaid
graph TB
    subgraph "Input Files (JSON)"
        Base[Base Path: test-report-YYYYMMDD_HHMMSS-servicename]

        Events[.events-throughput.json]
        API[.api-throughput.json]
        Service[.resource-metrics.json]
        RabbitMQ[.rabbitmq-metrics.json]
        Postgres[.postgres-metrics.json]
        System[.system-metrics.json]

        Base -.-> Events
        Base -.-> API
        Base -.-> Service
        Base -.-> RabbitMQ
        Base -.-> Postgres
        Base -.-> System
    end

    subgraph "ChartGenerator"
        Derive[Derive base path from chart.png path]
        Load[Load each JSON file]
        Deserialize[Deserialize to model objects]
        Generate[Generate chart using ScottPlot]
    end

    subgraph "Output File"
        PNG[.chart.png]
        Specs[2100×2400 pixels, 150 DPI equivalent]
        PNG -.-> Specs
    end

    Events --> Load
    API --> Load
    Service --> Load
    RabbitMQ --> Load
    Postgres --> Load
    System --> Load

    Base --> Derive
    Derive --> Load
    Load --> Deserialize
    Deserialize --> Generate
    Generate --> PNG

    style Base fill:#95a5a6,stroke:#7f8c8d,color:#fff
    style Derive fill:#3498db,stroke:#2980b9,color:#fff
    style Generate fill:#9b59b6,stroke:#8e44ad,color:#fff
    style PNG fill:#2ecc71,stroke:#27ae60,color:#fff
```

**Example File Paths:**

Given output path: `/workspace/test-results/test-report-20250113_142530-dotnet9aotreferenceservice.chart.png`

ChartGenerator derives:
- Base path: `/workspace/test-results/test-report-20250113_142530-dotnet9aotreferenceservice`
- Events: `{base}.events-throughput.json`
- API: `{base}.api-throughput.json`
- Service: `{base}.resource-metrics.json`
- RabbitMQ: `{base}.rabbitmq-metrics.json`
- Postgres: `{base}.postgres-metrics.json`
- System: `{base}.system-metrics.json`

---

## ScottPlot Integration Details

```mermaid
graph TB
    subgraph "ScottPlot 5.x API Usage"
        MultiPlot[MultiPlot rows:5, cols:1]

        subgraph "Subplot 1: Throughput"
            T1[GetSubplot 0,0]
            T2[Add.Scatter events]
            T3[Add.Scatter API]
            T4[Add.HorizontalLine avg]
            T5[Legend outside right]
        end

        subgraph "Subplots 2-5: Resources"
            R1[GetSubplot 1-4,0]
            R2[Add.Scatter CPU left axis]
            R3[Add.FillY CPU alpha 0.3]
            R4[Add.Scatter RAM right axis]
            R5[Add.FillY RAM alpha 0.2]
            R6[Add.HorizontalLine avg both]
            R7[Legend outside right]
        end

        subgraph "All Subplots"
            P1[Add.VerticalLine phase boundaries]
            P2[Add.Text phase labels top subplot]
            P3[Format X-axis bottom HH:MM:SS]
        end

        Export[SavePng 2100×2400]
        SkiaSharp[SkiaSharp Rendering]
        PNG[PNG File Output]

        MultiPlot --> T1
        T1 --> T2
        T2 --> T3
        T3 --> T4
        T4 --> T5

        MultiPlot --> R1
        R1 --> R2
        R2 --> R3
        R3 --> R4
        R4 --> R5
        R5 --> R6
        R6 --> R7

        T5 --> P1
        R7 --> P1
        P1 --> P2
        P2 --> P3
        P3 --> Export
        Export --> SkiaSharp
        SkiaSharp --> PNG
    end

    style MultiPlot fill:#e74c3c,stroke:#c0392b,color:#fff
    style Export fill:#9b59b6,stroke:#8e44ad,color:#fff
    style PNG fill:#2ecc71,stroke:#27ae60,color:#fff
```

**Key ScottPlot Patterns:**

1. **MultiPlot for Subplots:**
   ```csharp
   var multiPlot = new ScottPlot.MultiPlot(rows: 5, cols: 1);
   multiPlot.Width = 2100;
   multiPlot.Height = 2400;
   multiPlot.ShareX = true;
   ```

2. **Dual Y-Axes:**
   ```csharp
   // Left axis (CPU)
   var cpuScatter = plot.Add.Scatter(timestamps, cpuValues);
   cpuScatter.Axes.YAxis = plot.Axes.Left; // Default

   // Right axis (RAM)
   var ramScatter = plot.Add.Scatter(timestamps, ramValues);
   ramScatter.Axes.YAxis = plot.Axes.Right; // Explicit assignment
   ```

3. **Color with Alpha:**
   ```csharp
   var color = ScottPlot.Color.FromHex("#3498db");
   var fillColor = color.WithAlpha(0.3); // 30% opacity
   ```

4. **Legend Positioning:**
   ```csharp
   plot.Legend.IsVisible = true;
   plot.Legend.Location = Alignment.UpperLeft;
   plot.Legend.ManualOffsetX = plot.Width + 150; // Outside right edge
   ```

5. **Multiline Legend Text:**
   ```csharp
   scatter.LegendText = $"Avg: {avg:F1} %\n" +
                        $"Min: {min:F1} %\n" +
                        $"Max: {max:F1} %\n" +
                        $"Mode: {mode} %";
   ```

6. **Phase Boundaries:**
   ```csharp
   foreach (var plot in allPlots)
   {
       var vline = plot.Add.VerticalLine(timestamp);
       vline.Color = ChartColors.ConsumePhase;
       vline.LineStyle = LineStyle.Dotted;
       vline.LineWidth = 1.5;
       vline.Alpha = 0.6;
   }
   ```

7. **PNG Export:**
   ```csharp
   multiPlot.SavePng(outputPath);
   // Automatically uses configured Width/Height
   // 2100×2400 pixels = 14"×16" at 150 DPI
   ```

---

## Summary

This architecture document provides:

1. **Component Structure** - How ChartGenerator fits into the Reporting slice
2. **Data Flow** - How JSON data flows through the chart generation pipeline
3. **Dependencies** - External libraries and project references
4. **Sequence Diagrams** - Step-by-step execution flows
5. **Class Diagram** - Object relationships and responsibilities
6. **File System Interaction** - Input/output file patterns
7. **ScottPlot Integration** - Detailed library usage patterns

**Key Architectural Decisions:**

- **Single Responsibility:** ChartGenerator only generates charts; data collection handled by other slices
- **Graceful Degradation:** Missing data files result in partial charts, not complete failure
- **Exact Python Match:** All visual elements (colors, layout, formatting) match Python matplotlib output
- **Cross-Platform:** ScottPlot + SkiaSharp work on Windows, Linux, macOS (headless rendering)
- **Testability:** File-based assertions and indirect verification for chart validation

**Implementation Order:**

1. Create ChartColors helper class (static color definitions)
2. Implement IChartGenerator interface
3. Implement ChartGenerator class structure
4. Implement data loading methods (LoadThroughputReport, LoadResourceReport)
5. Implement plotting methods (PlotThroughputData, PlotResourceData)
6. Implement phase boundary methods (AddPhaseBoundaries, AddPhaseLabels)
7. Implement legend formatting methods
8. Implement main GenerateChartAsync method
9. Write integration tests following test guide
10. Visual comparison with Python output

---

**Last Updated:** 2025-01-14
**Status:** 📋 Architecture finalized, ready for implementation
**Related Documents:**
- Main Plan: `12.PHASE_3_REPORTING_PLAN.md`
- Test Guide: `12.PHASE_3_REPORTING_PLAN.CHARTGENERATOR_TESTS.md`
