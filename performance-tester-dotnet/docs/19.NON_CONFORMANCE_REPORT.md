# Contract Test Non-Conformance Report

**Generated:** 2026-01-23
**Test Summary:** 49 tests executed, 6 passed, 43 failed

This report documents all instances where the .NET `PerformanceTester.Reporting` module does not conform to the Python `performance-tester` output specification.

---

## Executive Summary

The .NET implementation has significant structural differences from the Python reference implementation. The main categories of non-conformance are:

1. **Field naming differences** - Using different JSON property names
2. **Missing fields** - Required fields not present in output
3. **Structural differences** - Different object hierarchy/organization
4. **Decimal precision** - Insufficient decimal places in some values

---

## 1. Main Report (`test-report-*.json`)

### 1.1 Missing `port` Field in `monitored_process`

**File:** `ReportGeneration/ReportGenerator.cs` or related model
**Expected (Python):**
```json
"monitored_process": {
  "pid": 12345,
  "name": "dotnet9ReferenceService",
  "port": 8093
}
```

**Actual (.NET):**
```json
"monitored_process": {
  "pid": 12345,
  "name": "dotnet9ReferenceService"
}
```

**Issue:** The `port` field is missing. The `MonitoredProcess` model in .NET does not include a `port` property.

---

### 1.2 Wrong Field Order in `monitored_process`

**Expected (Python) field order:** `pid`, `name`, `port`
**Actual (.NET) field order:** `pid`, `name`

**Issue:** Even ignoring the missing `port`, the test shows `name` at position 45 and `pid` at position 27, indicating the order is correct but the test may have expected a different order. The actual issue is the missing `port` field.

---

### 1.3 Decimal Precision for Throughput Values

**Field:** `results.phase1_publish.throughput_events_per_sec`
**Expected:** At least 2 decimal places (e.g., `11377.50`)
**Actual:** 1 decimal place (e.g., `11377.5`)

**Issue:** JSON serialization is not enforcing minimum decimal precision. Values like `11377.5` should be serialized as `11377.50` to match Python output.

---

## 2. Events Throughput (`*.events-throughput.json`)

### 2.1 Wrong Sample Field Names

**File:** `ThroughputGeneration/EventsThroughputReportGenerator.cs`

**Expected (Python) sample fields:**
```json
{
  "timestamp": "2026-01-22T11:41:55.150402",
  "elapsed_seconds": 0.088,
  "total_events": 23,
  "events_per_second": 261.23
}
```

**Actual (.NET) sample fields:**
```json
{
  "timestamp": "2026-01-22T11:41:55.150402",
  "elapsed_seconds": 0.088,
  "throughput_rate": 261.23,
  "cumulative_count": 23
}
```

**Issues:**
- `total_events` should be used instead of `cumulative_count`
- `events_per_second` should be used instead of `throughput_rate`

---

### 2.2 Wrong Summary Field Names

**Expected (Python) summary fields:**
- `avg_events_per_second`
- `peak_events_per_second`
- `min_events_per_second`
- `std_dev_events_per_second`
- `cv_events_per_second`
- `avg_response_time_ms`
- `total_samples`
- `total_events`

**Actual (.NET):** Different field names (likely `avg_rate`, `peak_rate`, etc.)

---

## 3. API Throughput (`*.api-throughput.json`)

### 3.1 Wrong Sample Field Names

**File:** `ThroughputGeneration/ApiThroughputReportGenerator.cs`

**Expected (Python) sample fields:**
```json
{
  "timestamp": "2026-01-22T11:42:04.123456",
  "elapsed_seconds": 1.0,
  "total_calls": 794,
  "calls_per_second": 794.0
}
```

**Actual (.NET) sample fields:**
```json
{
  "timestamp": "2026-01-22T11:42:04.123456",
  "elapsed_seconds": 1.0,
  "throughput_rate": 794.0,
  "cumulative_count": 794
}
```

**Issues:**
- `total_calls` should be used instead of `cumulative_count`
- `calls_per_second` should be used instead of `throughput_rate`

---

### 3.2 Wrong Summary Field Names

**Expected (Python) summary fields:**
- `avg_calls_per_second`
- `peak_calls_per_second`
- `min_calls_per_second`
- `std_dev_calls_per_second`
- `cv_calls_per_second`
- `avg_response_time_ms`
- `total_samples`
- `total_calls`

**Actual (.NET):** Different field names (likely generic rate-based names)

---

## 4. Resource Metrics (`*.resource-metrics.json`)

### 4.1 Missing `process_info` Object

**File:** `ResourceMetrics/ProcessResourceMetricsReportGenerator.cs`

**Expected (Python) root structure:**
```json
{
  "test_date": "2026-01-22 11:42:04",
  "process_info": {
    "pid": 12345,
    "name": "dotnet9ReferenceService",
    "port": 8092
  },
  "sampling_interval_ms": 500,
  "samples": [...],
  "summary": {...}
}
```

**Actual (.NET):** Missing or differently named `process_info` object.

---

### 4.2 Wrong Summary Field Names

**Expected (Python) summary fields:**
- `avg_cpu_percent`
- `peak_cpu_percent`
- `avg_memory_rss_mb`
- `peak_memory_rss_mb`
- `total_samples`

**Actual (.NET):** Likely missing `_rss_mb` suffix on memory fields.

---

## 5. System Metrics (`*.system-metrics.json`)

### 5.1 Missing `cpu_count` Field

**File:** `ResourceMetrics/SystemMetricsReportGenerator.cs`

**Expected (Python):**
```json
{
  "test_date": "2026-01-22 11:42:04",
  "cpu_count": 16,
  "sampling_interval_ms": 500,
  "samples": [...],
  "summary": {...},
  "is_wsl2": true
}
```

**Actual (.NET) available fields:** `test_date`, `sampling_interval_ms`, `samples`, `cpu_summary`, `memory_summary`

**Issues:**
- Missing `cpu_count` at root level
- Missing `is_wsl2` at root level
- Uses `cpu_summary` and `memory_summary` instead of single `summary` object

---

### 5.2 Missing `is_wsl2` Field

**Expected:** Boolean field `is_wsl2` at root level
**Actual:** Field not present

---

### 5.3 Wrong Sample Field Names

**Expected (Python) sample fields:**
- `timestamp`
- `cpu_percent`
- `memory_used_mb`
- `memory_total_mb`
- `memory_percent`

**Actual (.NET):** Uses `memory_mb` instead of `memory_used_mb`, `memory_total_mb`, `memory_percent`

---

### 5.4 Missing `min_cpu_percent` in Summary

**Expected:** `min_cpu_percent` field in summary (unique to system metrics)
**Actual:** Field not present

---

## 6. Container Metrics (RabbitMQ, PostgreSQL)

### 6.1 Missing `container_info` Object

**Files:**
- `ResourceMetrics/RabbitMqMetricsReportGenerator.cs`
- `ResourceMetrics/PostgresMetricsReportGenerator.cs`

**Expected (Python) root structure:**
```json
{
  "test_date": "2026-01-22 11:42:04",
  "container_info": {
    "name": "rabbitmq",
    "id": "abc123def456"
  },
  "sampling_interval_ms": 3000,
  "samples": [...],
  "summary": {...}
}
```

**Actual (.NET) available fields:** `test_date`, `sampling_interval_ms`, `samples`, `cpu_summary`, `memory_summary`

**Issues:**
- Missing `container_info` object with `name` and `id`
- Uses separate `cpu_summary` and `memory_summary` instead of single `summary` object

---

### 6.2 Wrong Summary Field Names

**Expected (Python) summary fields:**
- `avg_cpu_percent`
- `peak_cpu_percent`
- `avg_memory_mb`
- `peak_memory_mb`
- `total_samples`

**Actual (.NET):** Uses separate summary objects with different field structure.

---

## 7. Comparison Report (`*.md`)

### 7.1 Test Infrastructure Issues

The comparison report tests failed due to file permission issues (`UnauthorizedAccessException`) when trying to write to `/tmp/contract-test-*` directories. This is a test infrastructure issue, not a conformance issue with the business logic.

**Note:** The comparison report conformance cannot be fully evaluated until the test infrastructure issue is resolved.

---

## Summary of Required Changes

### High Priority (Core Structural Issues)

| File/Component | Issue | Required Change |
|----------------|-------|-----------------|
| `MonitoredProcess` model | Missing `port` field | Add `Port` property to model |
| Events throughput | Wrong field names | Rename `throughput_rate` → `events_per_second`, `cumulative_count` → `total_events` |
| API throughput | Wrong field names | Rename `throughput_rate` → `calls_per_second`, `cumulative_count` → `total_calls` |
| System metrics | Missing `cpu_count`, `is_wsl2` | Add fields to root output |
| Container metrics | Missing `container_info` | Add `container_info` object with `name`, `id` |
| All metrics | Separate summaries | Consolidate to single `summary` object |

### Medium Priority (Field Naming)

| Component | Current Name | Expected Name |
|-----------|--------------|---------------|
| Events summary | `avg_rate` | `avg_events_per_second` |
| Events summary | `peak_rate` | `peak_events_per_second` |
| Events summary | `min_rate` | `min_events_per_second` |
| API summary | `avg_rate` | `avg_calls_per_second` |
| API summary | `peak_rate` | `peak_calls_per_second` |
| System samples | `memory_mb` | `memory_used_mb`, `memory_total_mb`, `memory_percent` |
| Process summary | `avg_memory_mb` | `avg_memory_rss_mb` |
| Process summary | `peak_memory_mb` | `peak_memory_rss_mb` |

### Low Priority (Formatting)

| Component | Issue | Required Change |
|-----------|-------|-----------------|
| Decimal precision | Values like `11377.5` | Ensure minimum 2 decimal places: `11377.50` |

---

## Files Requiring Modification

Based on the analysis, the following files likely need modifications:

1. **Models:**
   - `Models/MonitoredProcess.cs` - Add `Port` property
   - `Models/ThroughputSample.cs` - Rename properties for events/API specific naming
   - `Models/SystemMetricsSample.cs` - Add memory breakdown fields
   - `Models/ContainerInfo.cs` - Create new model for container_info

2. **Report Generators:**
   - `ReportGeneration/ReportGenerator.cs` - Include port in monitored_process
   - `ThroughputGeneration/EventsThroughputReportGenerator.cs` - Use events-specific field names
   - `ThroughputGeneration/ApiThroughputReportGenerator.cs` - Use calls-specific field names
   - `ResourceMetrics/SystemMetricsReportGenerator.cs` - Add cpu_count, is_wsl2, restructure summary
   - `ResourceMetrics/RabbitMqMetricsReportGenerator.cs` - Add container_info, restructure summary
   - `ResourceMetrics/PostgresMetricsReportGenerator.cs` - Add container_info, restructure summary
   - `ResourceMetrics/ProcessResourceMetricsReportGenerator.cs` - Add process_info, use memory_rss_mb names

3. **JSON Serialization:**
   - Custom converters or settings to enforce decimal precision

---

## Test Results Summary

| Test Category | Passed | Failed | Total |
|---------------|--------|--------|-------|
| Main Report | 1 | 3 | 4 |
| Events Throughput | 0 | 7 | 7 |
| API Throughput | 0 | 5 | 5 |
| Resource Metrics | 2 | 4 | 6 |
| System Metrics | 0 | 6 | 6 |
| RabbitMQ Metrics | 2 | 3 | 5 |
| PostgreSQL Metrics | 1 | 4 | 5 |
| Comparison Report | 0 | 11 | 11 |
| **Total** | **6** | **43** | **49** |

---

## Conclusion

The .NET implementation has significant structural differences from the Python reference implementation. The primary issues are:

1. **Different field naming conventions** for throughput metrics (generic vs. domain-specific)
2. **Missing required fields** (`port`, `cpu_count`, `is_wsl2`, `container_info`)
3. **Different object structure** for summaries (separate cpu/memory vs. unified summary)
4. **Missing memory breakdown** in system metrics samples

These issues would need to be addressed in the business logic to achieve 100% compatibility with the Python output format.
