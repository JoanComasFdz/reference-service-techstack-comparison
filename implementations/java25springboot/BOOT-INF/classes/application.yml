server:
  port: 8101

spring:
  application:
    name: java25SpringBootReferenceService

  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
    listener:
      simple:
        prefetch: 10

  datasource:
    url: jdbc:postgresql://localhost:5432/java25springboot_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.joancomasfdz.performancetest: DEBUG
    org.springframework.amqp: DEBUG
